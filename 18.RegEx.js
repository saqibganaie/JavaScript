const regEx = /very/g; // this means very globally (i.e every very)
const str =
  "Saqib's interviews are going very very well and everyone is very very happy about it";

console.log(str.replace(regEx, "VERY"));

// * go to regexr.com for info about regular expression

// * Also learn more about regular expression and Event Loop
